server:
  port: 8888

management:
  endpoints:
    web:
      exposure:
        include: '*'

spring:
  profiles:
    active: git
  application:
    name: config-server
  cloud:
    config:
      override-system-properties: false
      server:
        git:
          uri: https://github.com/athulravindran87/spring-cloud-config.git
          clone-on-start: true
          search-paths:
            config-server/src/main/resources/config/,
            config-server/src/main/resources/config/client-service-1/,
            config-server/src/main/resources/config/client-service-2/
          username: prasanna29387
          password: Yamu2429$
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
  instance:
    hostname: ${spring.application.name}
    preferIpAddress: true